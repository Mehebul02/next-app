
export const metadata = {
    title:'Blog',
    description:'Blog page'
}

const BlogPage = () => {
    return (
        <div>
            <h1>Blog page</h1>
        </div>
    );
};

export default BlogPage;